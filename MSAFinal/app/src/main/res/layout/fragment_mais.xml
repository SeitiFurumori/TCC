<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mainActivy"
    tools:context=".MaisFragment">

    <Button
        android:id="@+id/buttonGerarPDF"
        android:layout_width="0dp"
        android:layout_height="75dp"
        android:layout_margin="20dp"
        android:text="Gerar PDF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/buttonGraficos"
        android:layout_width="0dp"
        android:layout_height="75dp"
        android:layout_marginTop="20dp"
        android:text="GRAFICO"
        app:layout_constraintEnd_toEndOf="@+id/buttonGerarPDF"
        app:layout_constraintStart_toStartOf="@+id/buttonGerarPDF"
        app:layout_constraintTop_toBottomOf="@+id/buttonGerarPDF" />

    <Button
        android:id="@+id/buttonReset"
        android:layout_width="0dp"
        android:layout_height="75dp"
        android:layout_marginTop="20dp"
        android:text="RESETAR O MÊS"
        app:layout_constraintEnd_toEndOf="@+id/buttonGraficos"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="@+id/buttonGraficos"
        app:layout_constraintTop_toBottomOf="@+id/buttonGraficos" />
</androidx.constraintlayout.widget.ConstraintLayout>